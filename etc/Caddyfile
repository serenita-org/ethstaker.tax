{
    admin caddy:2019
    servers {
        metrics
    }
}

ethstaker.tax http://localhost:80 {
    handle /api/* {
        reverse_proxy api:8000
    }

    reverse_proxy frontend:8000
}

eth2.tax {
    redir https://ethstaker.tax{uri} permanent
}
